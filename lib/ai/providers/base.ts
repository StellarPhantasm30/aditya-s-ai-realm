import type { Provider } from "./types";

// Common surface every provider implements. Kept intentionally small so
// swapping/adding providers requires only implementing three methods.
export type { Provider };
